<!-- This form takes user input and sends a POST request
   to the "gpt" route. The user's input is appended to the prompt, 
   and the OpenAI API generates a response based on the combined prompt and input. 
   The response is displayed below the form. -->
<!-- This section displays the response generated by the OpenAI API, if available. It includes both the prompt and the user's input, as well as the generated response. --> 
<!-- Tingwei Pu -->
<br>
<br>
<br>
<h4>Tingwei Pu ChatGPT Prompt:</h4>
<br>
<br>
<form method="post" action="gpt">
  <%= prompt %><br>
  <input type="text" name="input" placeholder=""><br>
  <input type="hidden" name="prompt" value='<%= prompt %>'>
  <br>
  <input type="submit" value="submit">
</form>
<br>
<% if (typeof response !== 'undefined' && typeof input !== 'undefined') { %>
  <p><%= prompt %><br> <br><br> <%= input %><br><br><br><%= response %></p>
<% } %>





